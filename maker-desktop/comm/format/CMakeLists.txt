if(DEFINED CONFIG_COMM_FORMAT_CUSTOM)
	file(GLOB_RECURSE SRC_CURRENT
		${CMAKE_CURRENT_SOURCE_DIR}/custom/*.c
	)
	set(HEAD_DIR_CURRENT
		${CMAKE_CURRENT_SOURCE_DIR}/custom
	)
elseif(DEFINED CONFIG_COMM_FORMAT_HID_CUSTOM)
	file(GLOB_RECURSE SRC_CURRENT
		${CMAKE_CURRENT_SOURCE_DIR}/hid/custom/*.c
		${CMAKE_CURRENT_SOURCE_DIR}/hid/hhid_r.c
		${CMAKE_CURRENT_SOURCE_DIR}/hid/hhid_w.c
	)
	set(HEAD_DIR_CURRENT
		${CMAKE_CURRENT_SOURCE_DIR}/hid
	)
elseif(DEFINED CONFIG_COMM_FORMAT_HID_CH9329)
	file(GLOB_RECURSE SRC_CURRENT
		${CMAKE_CURRENT_SOURCE_DIR}/hid/ch9329/*.c
		${CMAKE_CURRENT_SOURCE_DIR}/hid/hhid_r.c
		${CMAKE_CURRENT_SOURCE_DIR}/hid/hhid_w.c
	)
	set(HEAD_DIR_CURRENT
		${CMAKE_CURRENT_SOURCE_DIR}/hid
	)
endif()



set(SOURCES ${SOURCES}
	${SRC_CURRENT}
	PARENT_SCOPE
)
set(PUBLIC_HEADER ${PUBLIC_HEADER}
	${HEAD_DIR_CURRENT}
	PARENT_SCOPE
)
