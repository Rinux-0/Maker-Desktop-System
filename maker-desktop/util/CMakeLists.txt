if(DEFINED CONFIG_COMM_SPI)
	file(GLOB_RECURSE SRC_CURRENT ${SRC_CURRENT}
		${CMAKE_CURRENT_SOURCE_DIR}/color/*.c
	)
	set(HEAD_DIR_CURRENT
		${CMAKE_CURRENT_SOURCE_DIR}/color
	)
endif()

file(GLOB_RECURSE SRC_CURRENT ${SRC_CURRENT}
	${CMAKE_CURRENT_SOURCE_DIR}/tool/*.c
)
set(HEAD_DIR_CURRENT ${HEAD_DIR_CURRENT}
	${CMAKE_CURRENT_SOURCE_DIR}
	${CMAKE_CURRENT_SOURCE_DIR}/tool
)



set(SOURCES ${SOURCES}
	${SRC_CURRENT}
	PARENT_SCOPE
)
set(PUBLIC_HEADER ${PUBLIC_HEADER}
	${HEAD_DIR_CURRENT}
	PARENT_SCOPE
)
