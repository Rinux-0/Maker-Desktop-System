if(DEFINED CONFIG_DEVICE_DYNAMIC_FINGERPRINT)
	file(GLOB_RECURSE SRC_CURRENT_F
		${CMAKE_CURRENT_SOURCE_DIR}/fingerprint/*.c
	)
endif()
if(DEFINED CONFIG_DEVICE_DYNAMIC_KNOB)
	file(GLOB_RECURSE SRC_CURRENT_K
		${CMAKE_CURRENT_SOURCE_DIR}/knob/*.c
	)
endif()
if(DEFINED CONFIG_DEVICE_DYNAMIC_NFC)
	file(GLOB_RECURSE SRC_CURRENT_N
		${CMAKE_CURRENT_SOURCE_DIR}/nfc/*.c
	)
endif()



file(GLOB SRC_CURRENT
	${CMAKE_CURRENT_SOURCE_DIR}/*.c
)
set(SRC_CURRENT ${SRC_CURRENT}
	${SRC_CURRENT_F}
	${SRC_CURRENT_K}
	${SRC_CURRENT_N}
)

set(HEAD_DIR_CURRENT
	${CMAKE_CURRENT_SOURCE_DIR}
	${CMAKE_CURRENT_SOURCE_DIR}/fingerprint
	${CMAKE_CURRENT_SOURCE_DIR}/knob
	${CMAKE_CURRENT_SOURCE_DIR}/nfc
)



set(SOURCES ${SOURCES}
	${SRC_CURRENT}
	PARENT_SCOPE
)
set(PUBLIC_HEADER ${PUBLIC_HEADER}
	${HEAD_DIR_CURRENT}
	PARENT_SCOPE
)
