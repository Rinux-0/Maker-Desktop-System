if(DEFINED CONFIG_DEVICE_TRINITY_SOUND)
	file(GLOB_RECURSE SRC_CURRENT_S
		${CMAKE_CURRENT_SOURCE_DIR}/sound/*.c
	)
endif()
if(DEFINED CONFIG_DEVICE_TRINITY_USBHUB)
	file(GLOB_RECURSE SRC_CURRENT_U
		${CMAKE_CURRENT_SOURCE_DIR}/usb-hub/*.c
	)
endif()
if(DEFINED CONFIG_DEVICE_TRINITY_VFD)
	file(GLOB_RECURSE SRC_CURRENT_V
		${CMAKE_CURRENT_SOURCE_DIR}/vfd/*.c
	)
endif()



file(GLOB SRC_CURRENT
	${CMAKE_CURRENT_SOURCE_DIR}/*.c
)
set(SRC_CURRENT ${SRC_CURRENT}
	${SRC_CURRENT_S}
	${SRC_CURRENT_U}
	${SRC_CURRENT_V}
)

set(HEAD_DIR_CURRENT
	${CMAKE_CURRENT_SOURCE_DIR}
	${CMAKE_CURRENT_SOURCE_DIR}/sound
	${CMAKE_CURRENT_SOURCE_DIR}/usb-hub
	${CMAKE_CURRENT_SOURCE_DIR}/vfd
)



set(SOURCES ${SOURCES}
	${SRC_CURRENT}
	PARENT_SCOPE
)
set(PUBLIC_HEADER ${PUBLIC_HEADER}
	${HEAD_DIR_CURRENT}
	PARENT_SCOPE
)
