/*
	// 数据解包
	u8 diff[NUM_REGISTER] = {};	// 变化的键
	u8 down_new[NUM_KEY]  = {};	// 新按下的键
	u8 up_new  [NUM_KEY]  = {};	// 新释放的键
	for (u8 i=0; i<NUM_REGISTER; i++) {
		diff[i] = keypad_now[i] ^ keypad_past[i];	// 变化的键
		if (!diff[i])	// 无变化
			continue;
		down_new[i] = diff[i] & keypad_now[i];	// 新按下的键
		up_new  [i] = diff[i] & keypad_past[i];	// 新释放的键
	}
*/
