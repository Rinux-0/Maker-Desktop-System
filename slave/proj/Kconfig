orsource "./comm/Kconfig"





choice
    prompt "device / test"
    depends on ENABLE_SLAVE_SAMPLE
    default DEVICE

    config DEVICE
        bool "device"
        orsource "./device/Kconfig"

    config TEST
        bool "test"
        orsource "./test/Kconfig"
endchoice
